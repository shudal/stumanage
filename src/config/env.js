import base from '../untils/base'

let baseUrl = '';
let routerMode = 'hash';
let baseImgPath = '/static/assets/img/';
let uploadImgUrl = base.url + '/index/student/add';
console.log('e' + uploadImgUrl)

export {
	baseUrl,
	routerMode,
	baseImgPath,
  uploadImgUrl,
}
